{{! Combat Tab }}
<section class="tab {{tabs.combat.id}} {{tabs.combat.cssClass}}" data-tab="{{tabs.combat.id}}" data-group="{{tabs.combat.group}}" id="combat">
  {{! FIGHT }}
  <div>A fair fight</div>
</section>
{{! End Combat Tab }}